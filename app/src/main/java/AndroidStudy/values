values
- colors(색), strings(텍스트), styles(앱의 테마)가 있고 각각 하드 코딩으로 사용하는 값을
리소스로 사용할 수 있게 만든다.
- styles을 통해 테마를 특별히 새로 만들지 않는다. 기본 테마에 <item name="windowNoTitle">true</item>
(화면 상단에 보이는 앱의 이름이 나오지 않게 한다)를 추가하는 정도로 사용한다.